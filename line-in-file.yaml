---

  - name: Add two lines
    hosts: localhost
    tasks:
      - name: Add two lines in /tmp/file.txt
        blockinfile:
          path: /tmp/file.txt
          block: |
            This is line 1
            This is line 2

